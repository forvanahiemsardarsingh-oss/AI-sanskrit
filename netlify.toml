[build]
  publish = "."
  command = "echo 'Static site - no build needed'"

[[redirects]]
  from = "/"
  to = "/install.html"
  status = 200

[context.production.environment]
  NODE_ENV = "production"
